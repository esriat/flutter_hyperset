import 'package:flutter_hyperset/model/card_data/card.dart';

/// This represents the View of a Card
class CardView{
    /// This is the card that the CardView will represent.
    Card card;

    /// Ctor taking a Card as parameter.
    CardView(this.card);
}